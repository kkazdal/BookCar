@model List<GetTagByBlogIdDto>


<div class="sidebar-box ftco-animate">
    <h3>Etiket Bulut</h3>
    <div class="tagcloud">
        @foreach (var item in Model)
        {
            <a href="#" class="tag-cloud-link">@item.TagName</a>
        }
    </div>
</div>